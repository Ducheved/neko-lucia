{
  "name": "neko-lucia-monorepo",
  "private": true,
  "version": "0.0.0",
  "description": "neko-lucia — session library maintained by @Ducheved (Lucia v3 lineage)",
  "packageManager": "pnpm@9.15.9",
  "scripts": {
    "build": "pnpm -r --filter @ducheved/neko-lucia --filter @ducheved/neko-lucia-adapter-drizzle run build",
    "test": "pnpm --filter @ducheved/neko-lucia run test",
    "typecheck": "pnpm -r --filter @ducheved/neko-lucia --filter @ducheved/neko-lucia-adapter-drizzle exec tsc --noEmit"
  },
  "author": "Ducheved",
  "license": "MIT",
  "engines": {
    "node": ">=20.9.0",
    "pnpm": ">=9"
  }
}
